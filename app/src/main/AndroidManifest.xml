<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_scheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.e_scheduler">
        <activity
            android:name=".DashboardSideNavAvtivity"
            android:exported="false"
            android:label="@string/title_activity_dashboard_side_nav_avtivity"
            android:theme="@style/Theme.e_scheduler.NoActionBar" />
        <activity
            android:name=".AnnouncementActivity"
            android:exported="false" />
        <activity
            android:name=".NoteInfoActivity"
            android:exported="true" />
        <activity
            android:name=".NoticeActivity"
            android:exported="true" />

        <receiver
            android:name=".AlarmBroadcastReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".NotificationReceiver"
            android:enabled="true" />

        <service android:name=".AlarmService" />

        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />
    </application>

</manifest>